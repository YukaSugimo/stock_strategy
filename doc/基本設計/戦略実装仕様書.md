# 戦略実装仕様書

## 概要

新しい戦略を追加するときは `strategies/base.py` を継承したクラスを作る。
`backtest.py` と `optimize.py` は戦略の中身を知らない。
`generate_signal()` を実装するだけで自動的にバックテストに組み込まれる。

## ファイル構成

```
scripts/
├── strategies/
│   ├── base.py                   # 基底クラス（変更禁止）
│   ├── s01_rsi_macd_bb.py        # 現在のロジック（動作確認済み）
│   ├── s02_macd_position.py      # MACDゾーンフィルター（未実装）
│   └── sXX_name.py               # 追加する戦略
└── params/
    ├── s01_rsi_macd_bb.yaml      # 固定パラメータ
    ├── s01_rsi_macd_bb_grid.yaml # グリッドサーチ用
    └── sXX_name.yaml
```

## base.pyの仕様

```python
# scripts/strategies/base.py
from abc import ABC, abstractmethod
import pandas as pd

class BaseStrategy(ABC):

    def __init__(self, params: dict):
        self.params = params

    @abstractmethod
    def generate_signal(
        self,
        close:  pd.Series,
        volume: pd.Series = None,
    ) -> dict:
        """
        シグナルを生成して返す。

        Args:
            close:  終値のSeries（インデックスは日付）
            volume: 出来高のSeries（使わない戦略はNoneのまま）

        Returns:
            {
                "signal":     "buy" / "sell" / None,
                "status":     "confirmed" / "candidate" / "none",
                "indicators": {
                    "rsi":   float,   # 使った指標値（デバッグ・DB保存用）
                    "macd":  float,
                    ...
                }
            }
        """
        pass

    @property
    def name(self) -> str:
        return self.__class__.__name__
```

## 実装例

```python
# scripts/strategies/s01_rsi_macd_bb.py
import sys
from pathlib import Path
import pandas as pd

sys.path.insert(0, str(Path(__file__).parent.parent))
from strategies.base import BaseStrategy
from indicators import calc_rsi, calc_macd, calc_bb

class S01RsiMacdBb(BaseStrategy):

    def generate_signal(self, close: pd.Series, volume=None) -> dict:
        p = self.params

        rsi  = calc_rsi(close,  period=p.get("rsi_period", 14))
        macd = calc_macd(close, fast=p.get("macd_fast", 12),
                                slow=p.get("macd_slow", 26),
                                signal_span=p.get("macd_signal", 9))
        bb   = calc_bb(close,   period=p.get("bb_period", 20),
                                k=p.get("bb_std", 2.0))

        buy_threshold  = p.get("rsi_buy_threshold",  30)
        sell_threshold = p.get("rsi_sell_threshold", 70)

        signals = {}
        if rsi.prev <= buy_threshold and rsi.signal == "buy":
            signals["RSI"] = "buy"
        if rsi.prev >= sell_threshold and rsi.signal == "sell":
            signals["RSI"] = "sell"
        if macd.signal:
            signals["MACD"] = macd.signal
        if bb.signal:
            signals["BB"] = bb.signal

        entry = p.get("entry_condition", "3of3")
        threshold = 3 if entry == "3of3" else 2

        buy_count  = sum(1 for v in signals.values() if v == "buy")
        sell_count = sum(1 for v in signals.values() if v == "sell")

        if buy_count >= threshold:
            return {
                "signal": "buy",
                "status": "confirmed" if buy_count == 3 else "candidate",
                "indicators": {
                    "rsi":      rsi.value,
                    "macd":     macd.macd,
                    "bb_pct_b": bb.pct_b,
                },
            }
        elif sell_count >= threshold:
            return {
                "signal": "sell",
                "status": "confirmed" if sell_count == 3 else "candidate",
                "indicators": {
                    "rsi":      rsi.value,
                    "macd":     macd.macd,
                    "bb_pct_b": bb.pct_b,
                },
            }
        else:
            return {
                "signal": None,
                "status": "none",
                "indicators": {
                    "rsi":      rsi.value,
                    "macd":     macd.macd,
                    "bb_pct_b": bb.pct_b,
                },
            }
```

## パラメータファイルの書き方

### 固定パラメータ（sXX_name.yaml）

```yaml
name: s01_rsi_macd_bb
description: RSI/MACD/BBの3指標一致ルール（基準戦略）

rsi_period:          14
rsi_buy_threshold:   30
rsi_sell_threshold:  70
macd_fast:           12
macd_slow:           26
macd_signal:         9
bb_period:           20
bb_std:              2.0
entry_condition:     3of3
```

### グリッドサーチ用（sXX_name_grid.yaml）

```yaml
rsi_buy_threshold:  [25, 30, 35, 40]
rsi_sell_threshold: [60, 65, 70, 75]
bb_std:             [1.5, 2.0, 2.5]
entry_condition:    ["2of3", "3of3"]
```

リスト形式で書いた値が全組み合わせで試される。
書いていないパラメータは固定パラメータの値をそのまま使う。

## 戦略追加時のチェックリスト

- [ ] `strategies/sXX_name.py` を作成した
- [ ] `BaseStrategy` を継承している
- [ ] `generate_signal()` が `signal / status / indicators` を返す
- [ ] `params/sXX_name.yaml` を作成した
- [ ] `params/sXX_name_grid.yaml` を作成した
- [ ] 単体動作確認を実行した（下記コマンド参照）
- [ ] バックテストが動くことを確認した
- [ ] Gitでコミットした（戦略バージョン管理のため必須）

### 単体動作確認の手順

`backtest.py` 改修前の現状では以下で確認する：

```powershell
# 1. データが存在することを確認
python scripts/fetch_data.py --tickers 7203.T --days 60

# 2. 指標計算が動くことを確認（indicators.pyは戦略のベースとなるライブラリ）
python scripts/indicators.py --ticker 7203.T

# 3. 戦略クラスを直接instantiateしてシグナルが返ることを確認
python -c "
import pandas as pd, sys
sys.path.insert(0, 'scripts')
from strategies.s01_rsi_macd_bb import S01RsiMacdBb
import yaml

with open('params/s01_rsi_macd_bb.yaml', encoding='utf-8') as f:
    params = yaml.safe_load(f)

df = pd.read_csv('data/raw/20260414/7203.T.csv', index_col=0, parse_dates=True)
close = df['Close'].astype(float)

strategy = S01RsiMacdBb(params)
result = strategy.generate_signal(close)
print(result)
"
```

## 命名規則

| 項目 | 規則 | 例 |
|------|------|-----|
| ファイル名 | `sXX_説明.py` | `s03_adx_filter.py` |
| クラス名 | ファイル名をキャメルケースに | `S03AdxFilter` |
| パラメータファイル | ファイル名と同じ | `s03_adx_filter.yaml` |
| グリッドファイル | `_grid` を末尾に追加 | `s03_adx_filter_grid.yaml` |
| 番号 | 2桁の連番 | s01, s02, s03... |
